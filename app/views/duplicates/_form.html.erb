<%= form_for(@duplicate_group) do |f| %>
  <% if @duplicate_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@duplicate_group.errors.count, "error") %> prohibited this duplicate_group from being saved:</h2>

      <ul>
      <% @duplicate_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
