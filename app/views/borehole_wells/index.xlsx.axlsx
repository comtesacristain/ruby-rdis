wb = xlsx_package.workbook

wb.add_worksheet(name: "Wells") do |sheet|
  sheet.add_row @columns.map {|column| column.upcase}
 
 
 
  @borehole_wells.each do |borehole_well|
  	sheet.add_row @columns.map { |column|  borehole_well[column] }
  end
end
